  variables:
  - message: Hello world
  
  stages:
  - stage: first-stage
    jobs:
    -job: first-job
      steps: 
        -script: |echo ${{ message }}
    -job: second-job
      steps: 
        -script: |echo ${{ message }}

  - stage: second-stage
    jobs:
    -job: first-job
      steps: 
        -script: |echo ${{ message }}
    -job: second-job
      steps: 
        -script: |echo ${{ message }}
