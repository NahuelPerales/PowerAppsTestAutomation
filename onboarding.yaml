  variables:
  - message: Hello world
  
  stages:
  - stage: first-stage
    jobs:
    -job: 
      name: first-job
      steps: 
        -script: echo ${{ message }}
    -job: 
      name: second-job
      steps: 
        -script: echo ${{ message }}

  - stage: second-stage
    jobs:
    -job: 
      name: first-job
      steps: 
        -script: echo ${{ message }}
    -job: 
      name: second-job
      steps: 
        -script: echo ${{ message }}
